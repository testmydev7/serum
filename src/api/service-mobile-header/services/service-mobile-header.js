'use strict';

/**
 * service-mobile-header service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::service-mobile-header.service-mobile-header');
