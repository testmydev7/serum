'use strict';

/**
 * service-mobile-header controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::service-mobile-header.service-mobile-header');
