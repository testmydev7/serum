'use strict';

/**
 * service-mobile-header router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::service-mobile-header.service-mobile-header');
