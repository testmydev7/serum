'use strict';

/**
 * training-mobile-header router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::training-mobile-header.training-mobile-header');
